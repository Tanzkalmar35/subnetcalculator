/**
 *
 *
 * An example of calling a rust function.
 *
 * In this example:
 * "greet" - The name of the rust function called.
 * "name: 'testName'" - defines the arg that the rust function takes.
 *
 * async function greet() {
 *      // Learn more about Tauri commands at https://tauri.app/v1/guides/features/command
 *      await invoke("greet", {
 *          name: "testName",
 *      });
 * }
 */
